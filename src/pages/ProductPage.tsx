import { ArrowRight, Cube, Plus, UserCirclePlus } from "@phosphor-icons/react"
import { useMemo, useState } from "react"
import { Link } from "react-router-dom"
import { CreateAppPartModal } from "../components/forms/CreateAppPartModal"
import { AvatarGroup } from "../components/ui/Avatar"
import { StatusBadge } from "../components/ui/StatusBadge"
import { useWorkspace } from "../state/WorkspaceContext"

export function ProductPage() {
  const { projects, appParts, features, users, claimAppPart } = useWorkspace()
  const [createOpen, setCreateOpen] = useState(false)
  const [projectFilter, setProjectFilter] = useState("Alle")
  const visibleProjects = useMemo(() => projects.filter((project) => projectFilter === "Alle" || project.id === projectFilter), [projectFilter, projects])

  return <div className="page product-page"><div className="page-header"><div><h1>Das Produkt</h1><p>Alle dauerhaften Teile deiner Apps, ihre Stabilität und die Personen, die gerade daran arbeiten.</p></div><button className="button primary" type="button" onClick={() => setCreateOpen(true)}><Plus size={16} weight="bold" />App Teil erstellen</button></div><div className="product-overview"><div><strong>{appParts.length}</strong><span>App Teile</span></div><div><strong>{appParts.filter((appPart) => appPart.activeUserIds.length).length}</strong><span>In Bearbeitung</span></div><div><strong>{appParts.filter((appPart) => appPart.releaseState === "Production Ready").length}</strong><span>Production Ready</span></div><div><strong>{appParts.filter((appPart) => !appPart.ownerUserId).length}</strong><span>Frei</span></div></div><div className="product-filter"><label htmlFor="product-project-filter">Projekt</label><select id="product-project-filter" value={projectFilter} onChange={(event) => setProjectFilter(event.target.value)}><option>Alle</option>{projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</select></div><div className="product-projects">{visibleProjects.map((project) => { const parts = appParts.filter((appPart) => appPart.projectId === project.id); return <section className="product-project" key={project.id}><div className="product-project-heading"><span className="project-icon-large" style={{ background: project.color }}>{project.icon}</span><div><h2>{project.name}</h2><span>{project.type} <span className="inline-separator">/</span> {parts.length} App Teile</span></div><Link to={`/projects/${project.id}`}>Features ansehen <ArrowRight size={14} /></Link></div>{parts.length ? <div className="app-part-list">{parts.map((appPart) => { const activeUsers = users.filter((user) => appPart.activeUserIds.includes(user.id)); const linkedFeatures = features.filter((feature) => feature.appPartId === appPart.id); const owner = users.find((user) => user.id === appPart.ownerUserId); return <article className="app-part-row" key={appPart.id}><Link className="app-part-row-link" to={`/product/app-parts/${appPart.id}`}><span className="app-part-symbol"><Cube size={17} /></span><div className="app-part-copy"><span>{appPart.key} <span className="inline-separator">/</span> {appPart.platform}</span><strong>{appPart.name}</strong><p>{appPart.description}</p></div><StatusBadge value={appPart.releaseState} /><div className="app-part-feature-count"><strong>{linkedFeatures.length}</strong><span>{linkedFeatures.length === 1 ? "Feature" : "Features"}</span></div><div className="app-part-people">{activeUsers.length ? <><AvatarGroup users={activeUsers} /><span>Arbeitet gerade</span></> : owner ? <><AvatarGroup users={[owner]} /><span>Verantwortlich</span></> : <span>Niemand aktiv</span>}</div><ArrowRight size={15} /></Link>{!appPart.ownerUserId && <button className="claim-inline" type="button" onClick={() => claimAppPart(appPart.id)}><UserCirclePlus size={15} />Beanspruchen</button>}</article> })}</div> : <div className="empty-state product-empty"><Cube size={26} /><strong>Noch keine App Teile</strong><span>Lege den ersten dauerhaften Produktbereich für dieses Projekt an.</span><button className="button secondary" type="button" onClick={() => setCreateOpen(true)}>App Teil erstellen</button></div>}</section> })}</div><CreateAppPartModal open={createOpen} onClose={() => setCreateOpen(false)} /></div>
}
